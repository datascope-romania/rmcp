{
  "name": "@rmcp/bridge",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit", "build": "node build.mjs" },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.700.0",
    "@rmcp/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0", "vitest": "^3.0.0", "@types/node": "^22.0.0",
    "aws-sdk-client-mock": "^4.1.0", "esbuild": "^0.25.0"
  }
}
